/**
 * @function createPhoneNumber should create a valid US phone number and return it as a string.
 *
 * @param {nums} nums may be an array or a number, your algorithm should compensate for both cases.
 */

const createPhoneNumber = nums => {}

module.exports = createPhoneNumber
